# Dependencias necesarias para compilar con Auto-Py-to-EXE
# Instala estas dependencias antes de compilar

# Dependencias principales
tkinter
sqlite3
datetime
os
sys
threading
subprocess
shutil
tempfile
hashlib
base64
io
calendar

# Dependencias para reportes y gráficos
reportlab>=3.6.0
matplotlib>=3.5.0
pandas>=1.3.0
openpyxl>=3.0.0

# Dependencias para texto a voz (opcional)
pyttsx3>=2.90

# Dependencias para impresión (opcional)
python-escpos>=3.0
pyusb>=1.2.0
pywin32>=227

# Dependencias para imágenes
Pillow>=8.0.0

# PyInstaller y Auto-Py-to-EXE
pyinstaller>=4.10
auto-py-to-exe>=2.20.0